import styles from './Filter.scss';

const CLASSES = {
  active: `${styles.position} ${styles.filter} ${styles.filterActive}`,
  inactive: `${styles.position} ${styles.filter}`,
};

export default CLASSES;
